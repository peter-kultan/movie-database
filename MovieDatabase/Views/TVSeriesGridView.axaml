<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
             x:Class="MovieDatabase.Views.TVSeriesGridView">
	<Grid>
		<ListBox SelectedItem="{Binding SelectedTVSeries}" HorizontalAlignment="Center" ScrollViewer.HorizontalScrollBarVisibility="Disabled" BorderThickness="0" Items="{Binding TVSeries}" Background="Transparent" Margin="5 5">
			<ListBox.ItemsPanel>
				<ItemsPanelTemplate>
					<WrapPanel/>
				</ItemsPanelTemplate>
			</ListBox.ItemsPanel>
		</ListBox>

		<StackPanel Orientation="Vertical" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="10 10" Spacing="20">
			<Button Command="{Binding OpenTVSeriesDetailsCommand}" IsEnabled="{Binding IsSelectedTVSeries}">
				<PathIcon Data="{StaticResource info_regular}"/>
			</Button>
			<Button Command="{Binding ReloadTVs}">
				<PathIcon Data="{StaticResource arrow_counterclockwise_regular}"/>
			</Button>
		</StackPanel>
	</Grid>
</UserControl>
